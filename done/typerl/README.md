# typerl

Terminal typing trainer written in Perl (Curses).

Run: `perl typerl.pl`

- `config/` — options (timer, word quantity, layout...). Copy `config.example` to `config.pm`.
- `dict/` — word lists (english, spanish, perl).
